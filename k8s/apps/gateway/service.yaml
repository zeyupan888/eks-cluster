apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: llm
  labels:
    app: llm-gateway
spec:
  selector:
    app: llm-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8080
